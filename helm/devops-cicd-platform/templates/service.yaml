apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-cicd-platform.fullname" . }}
  labels:
    {{- include "devops-cicd-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-cicd-platform.selectorLabels" . | nindent 4 }}
