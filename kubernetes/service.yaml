apiVersion: v1
kind: Service
metadata:
  name: devops-cicd-platform-service
spec:
  type: NodePort
  selector:
    app: devops-cicd-platform
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
